@model NavbarModel
<div class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/Home/Index"><img src="/Content/img/logo.png" width="170" ></a>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-left help-me-cl">
                <li class="dropdown">
                    <a id="drop1" href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <div id="mobile_nav_icon"></div>
                         Меню
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a href="@Url.Action("Index","Project")">Проекты</a></li>
                        <li class="divider"></li>
                        <li><a href="@Url.Action("Index","GoodDeed")">Добрые дела</a></li>
                        <li class="divider"></li>
                        <li><a href="@Url.Action("OrganizationPage","User")">Волонтёрские организации</a></li>
                        <li class="divider"></li>
                        <li><a href="@Url.Action("VolunteerPage","User")">Волонтёры</a></li>
                    </ul>
                </li>
                <li>
                    @*<div class="d7">
                        <form>
                            <input type="text" placeholder="Поиск" text_color="black">
                            <a href="@Url.Action("Search","Shop")"class="glyphicon glyphicon-search" aria-hidden="true" style="color: black;"></a>
                        </form>
                    </div>*@
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li>
                    @*@if (User.Identity.IsAuthenticated)
                    {
                        <a>@Model.User.Email</a>
                    }*@

                    @if (User.Identity.IsAuthenticated)
                    {
                        if (Model.User.Role == "admin")
                        {
                        <li><a href="@Url.Action("Index", "Admin")">Панель админа</a></li>
                    }
                    else
                    if (Model.User.Role == "organization")
                    {
                        <li><a class="help-me-cl" href="@Url.Action("AddProject", "Organization")">Добавить проект</a></li>
                    }
                    else if (Model.User.Role == "volunteer")
                    {
                        <li><a class="help-me-cl" href="#">Добавить доброе дело</a></li>
                    }
                    else if (Model.User.Role == "user")
                    {
                        <li><a class="help-me-cl" href="@Url.Action("AddGoodDeed", "SimpleUser")">Попросить о помощи</a></li>
                    }

                    <li class="dropdown">
                        <a id="drop1" href="#" class="dropdown-toggle" data-toggle="dropdown">
                            <svg width="1.4em" height="1.2em" viewBox="0 0 16 16" class="bi bi-caret-down-square" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
                                <path fill-rule="evenodd" d="M3.544 6.295A.5.5 0 0 1 4 6h8a.5.5 0 0 1 .374.832l-4 4.5a.5.5 0 0 1-.748 0l-4-4.5a.5.5 0 0 1-.082-.537z" />
                                <path fill-rule="evenodd" d="M14 1H2a1 1 0 0 0-1 1v12a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1V2a1 1 0 0 0-1-1zM2 0a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V2a2 2 0 0 0-2-2H2z" />
                            </svg>
                        </a>
                        <ul class="dropdown-menu">
                            @if (Model.User.Role == "organization")
                            {
                                <li><a href="@Url.Action("MyProjects", "Organization")">Мои проекты</a></li>
                                <li class="divider"></li>
                            }

                            @if (Model.User.Role == "volunteer")
                            {
                                <li><a href="@Url.Action("MyParticipations", "Volunteer", new { email = User.Identity.Name })">Мои участия</a></li>
                                <li class="divider"></li>
                            }

                            <li><a href="@Url.Action("Logoff", "Account")">Выйти &nbsp;&nbsp;<i class="fa fa-sign-out" aria-hidden="true"></i></a></li>
                        </ul>
                    </li>
                }

                else
                {
                    <li><a href="@Url.Action("Login", "Account")">Войти</a></li>
                    <li><a href="/Account/RegisterType">Зарегистрироваться</a></li>
                }
                </ul>
            </div>
            <!--/.nav-collapse -->
        </div>
    </div>
